---
phase: 04-按钮启用与健壮性
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - coolcollege-unlock.user.js
autonomous: true
requirements: [BTN-01, BTN-02, BTN-03, BTN-04, BTN-05, ERR-01, ERR-02, ERR-03]

must_haves:
  truths:
    - "show_record 为 false 的灰色作答详情按钮自动变为蓝色可点击"
    - "点击解锁后的按钮在新标签页打开正确的作答详情页"
    - "跳转 URL 包含正确的 exam_id、submit_id、task_id、eid 参数"
    - "Fiber 数据不存在或 submit_id 缺失时脚本不报错，仅跳过并输出警告"
    - "页面结构变化导致选择器失效时输出控制台警告，不影响页面"
  artifacts:
    - path: "coolcollege-unlock.user.js"
      provides: "unlockDetailButton 函数 + processRow 按钮处理扩展"
      contains: "unlockDetailButton"
  key_links:
    - from: "processRow(row)"
      to: "unlockDetailButton(row, data)"
      via: "rowDataMap.get(row) 获取数据后调用"
      pattern: "unlockDetailButton\\(row"
    - from: "unlockDetailButton"
      to: "GM_openInTab"
      via: "点击事件触发跳转"
      pattern: "GM_openInTab\\(url"
---

<objective>
在 processRow 末尾添加按钮解锁逻辑：检测 show_record === "false" 的行，将灰色「作答详情」span 改为蓝色可点击，绑定点击事件通过 GM_openInTab 打开作答详情页。同时添加完善的防御性错误处理。

Purpose: 这是最终交付阶段 — 实现脚本的核心功能（按钮解锁+跳转）并确保异常情况不破坏页面
Output: 完整可用的作答详情解锁功能
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ReactFiber数据提取/03-01-SUMMARY.md

@coolcollege-unlock.user.js

<interfaces>
<!-- Phase 4 需要从 Phase 3 产出的接口和数据结构 -->

rowDataMap 数据结构 (Phase 03-01 SUMMARY):
```javascript
const rowDataMap = new Map();
// rowDataMap.set(row, { record, eid, examId });
// record 包含: submit_id, show_record, task_id 等
```

processRow(row) 当前实现 (Phase 3 产出):
```javascript
function processRow(row) {
  const record = getRecordFromRow(row);
  if (!record) return;                     // ERR-01: Fiber 不存在时跳过
  if (!record.submit_id) {                 // ERR-02: submit_id 缺失时跳过
    console.warn(`[${SCRIPT_NAME}] record 缺少 submit_id，跳过行`);
    return;
  }
  const eid = getEid();
  const examId = getExamId();
  rowDataMap.set(row, { record, eid, examId });
  // Phase 4 在此处添加按钮处理逻辑
}
```

GM_openInTab API (已在 metadata 中 @grant):
```javascript
GM_openInTab(url, { active: true });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 添加 unlockDetailButton 函数并在 processRow 中调用</name>
  <files>coolcollege-unlock.user.js</files>
  <action>
在 `coolcollege-unlock.user.js` 中添加按钮解锁函数，并修改 processRow 在数据提取成功后调用它。

**Step 1: 添加 unlockDetailButton 函数**

在 `processRow` 函数之后、`processRows` 函数之前（约第 166 行附近），添加以下函数：

```javascript
/**
 * 解锁「作答详情」按钮：修改样式 + 绑定点击跳转 (BTN-01~05, D-03~06)
 * @param {HTMLElement} row - 表格行元素
 * @param {{ record: Object, eid: string|null, examId: string|null }} data - 暂存数据
 */
function unlockDetailButton(row, data) {
  const { record, eid, examId } = data;

  // BTN-01: 仅处理 show_record === "false" 的行 (D-03)
  if (record.show_record !== "false") return;

  // D-01, D-02: 定位「作答详情」span — 通过文本内容而非类名
  const fixRightCell = row.querySelector('.ant-table-cell-fix-right');
  if (!fixRightCell) {
    console.warn(`[${SCRIPT_NAME}] 未找到 .ant-table-cell-fix-right 单元格，可能页面结构已变化`);
    return;
  }

  // 查找包含"作答详情"文本的 span
  const spans = fixRightCell.querySelectorAll('span');
  let detailSpan = null;
  for (const span of spans) {
    if (span.textContent.trim() === '作答详情') {
      detailSpan = span;
      break;
    }
  }

  if (!detailSpan) {
    console.warn(`[${SCRIPT_NAME}] 未找到「作答详情」span，可能页面结构已变化`);
    return;
  }

  // BTN-02: 修改样式为蓝色可点击 (D-03, D-04)
  detailSpan.style.color = 'rgb(0, 122, 255)';
  detailSpan.style.cursor = 'pointer';

  // BTN-03, BTN-04: 绑定点击事件，GM_openInTab 打开新标签页 (D-05)
  detailSpan.addEventListener('click', function (e) {
    e.stopPropagation();

    // BTN-05: 构造跳转 URL (D-06)
    // 格式: /training/examination/exam-detail?exam_id=xxx&submit_id=xxx&task_id=xxx&eid=xxx
    const params = new URLSearchParams();
    if (examId) params.set('exam_id', examId);
    if (record.submit_id) params.set('submit_id', record.submit_id);
    if (record.task_id) params.set('task_id', record.task_id);
    if (eid) params.set('eid', eid);

    const url = `https://pro.coolcollege.cn/training/examination/exam-detail?${params.toString()}`;

    console.log(`[${SCRIPT_NAME}] 打开作答详情: ${url}`);
    GM_openInTab(url, { active: true });
  });

  console.log(
    `[${SCRIPT_NAME}] 已解锁按钮: submit_id=${record.submit_id}`
  );
}
```

**Step 2: 修改 processRow 函数**

在 `processRow` 函数中，`rowDataMap.set(row, { record, eid, examId });` 这行之后（约第 159 行后），添加按钮处理调用：

```javascript
    // Phase 4: 解锁按钮 (BTN-01~05)
    unlockDetailButton(row, { record, eid, examId });
```

具体的 processRow 函数修改后的完整代码：

```javascript
  function processRow(row) {
    const record = getRecordFromRow(row);
    if (!record) return;

    // 检查关键字段 submit_id (DATA-03)
    if (!record.submit_id) {
      console.warn(`[${SCRIPT_NAME}] record 缺少 submit_id，跳过行`);
      return;
    }

    const eid = getEid();
    const examId = getExamId();

    // 暂存到 Map (D-07)
    rowDataMap.set(row, { record, eid, examId });

    // Phase 4: 解锁按钮 (BTN-01~05)
    unlockDetailButton(row, { record, eid, examId });

    console.log(
      `[SCRIPT_NAME}] 提取成功: submit_id=${record.submit_id}, ` +
      `show_record=${record.show_record}, eid=${eid}, examId=${examId}`
    );
  }
```

注意保留 `rowDataMap.set` 调用不变（Phase 3 的数据暂存机制），仅在后面追加 `unlockDetailButton` 调用。
  </action>
  <verify>
    <automated>node -c coolcollege-unlock.user.js && echo "Syntax OK"</automated>
  </verify>
  <done>
    1. unlockDetailButton 函数已添加到 coolcollege-unlock.user.js
    2. processRow 末尾调用了 unlockDetailButton
    3. 函数实现了：show_record==="false" 检测、样式修改为蓝色可点击、GM_openInTab 跳转
    4. 跳转 URL 包含 exam_id、submit_id、task_id、eid 参数
    5. JavaScript 语法检查通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 端到端验证 — 代码完整性检查与错误处理审查</name>
  <files>coolcollege-unlock.user.js</files>
  <action>
验证完整脚本的正确性和健壮性。此任务不修改代码，仅通过读取文件和语法检查确认 Task 1 的产出符合所有需求。

**验证清单（逐项确认）：**

1. **BTN-01**: unlockDetailButton 中有 `record.show_record !== "false"` 判断 — 仅处理灰色按钮行

2. **BTN-02**: 样式修改代码存在：`detailSpan.style.color = 'rgb(0, 122, 255)'` 和 `detailSpan.style.cursor = 'pointer'`

3. **BTN-03**: `detailSpan.addEventListener('click', ...)` 存在

4. **BTN-04**: 点击回调中有 `GM_openInTab(url, { active: true })` 调用

5. **BTN-05**: URL 构造使用 URLSearchParams，包含 exam_id、submit_id、task_id、eid 四个参数

6. **ERR-01**: processRow 中 `getRecordFromRow` 返回 null 时直接 return（Phase 3 已实现，确认未破坏）

7. **ERR-02**: processRow 中 `record.submit_id` 缺失时输出警告并 return（Phase 3 已实现，确认未破坏）

8. **ERR-03**: unlockDetailButton 中对 `.ant-table-cell-fix-right` 和「作答详情」span 的查找失败均有 console.warn 输出

9. **整体一致性**: metadata 中 `@grant GM_openInTab` 已存在（Phase 1），无需修改

10. **语法检查**: `node -c coolcollege-unlock.user.js` 通过

运行以下命令进行验证：
```bash
node -c coolcollege-unlock.user.js
grep -c "unlockDetailButton" coolcollege-unlock.user.js
grep -c "GM_openInTab" coolcollege-unlock.user.js
grep -c "show_record" coolcollege-unlock.user.js
grep "exam-detail" coolcollege-unlock.user.js
```
  </action>
  <verify>
    <automated>node -c coolcollege-unlock.user.js && grep -q "unlockDetailButton" coolcollege-unlock.user.js && grep -q "GM_openInTab" coolcollege-unlock.user.js && grep -q "exam-detail" coolcollege-unlock.user.js && echo "All checks passed"</automated>
  </verify>
  <done>
    1. JavaScript 语法检查通过
    2. unlockDetailButton 函数存在于脚本中
    3. GM_openInTab 调用存在
    4. 跳转 URL 包含 exam-detail 路径
    5. 所有 8 个需求 (BTN-01~05, ERR-01~03) 均有对应实现
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 页面 DOM -> 脚本选择器 | DOM 结构可能因网站更新而变化，选择器可能失效 |
| record 数据 -> URL 构造 | record 字段值未校验，可能包含意外值 |
| 脚本 -> 新标签页 | GM_openInTab 打开用户可控参数构造的 URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | URL 参数构造 | accept | URL 使用 URLSearchParams 标准构造，参数来自页面内部 Fiber 数据（非用户输入），风险极低 |
| T-04-02 | DoS | MutationObserver 回调 | mitigate | Phase 2 已实现 300ms 去抖 + data-processed 去重，防止重复处理 |
| T-04-03 | Information Disclosure | console.log 输出 | accept | 日志仅包含 submit_id 等业务标识，无敏感个人信息，在 Tampermonkey 沙箱环境内运行 |
</threat_model>

<verification>
完整脚本验证：
1. `node -c coolcollege-unlock.user.js` — JavaScript 语法正确
2. `grep "unlockDetailButton" coolcollege-unlock.user.js` — 新函数存在
3. `grep "GM_openInTab" coolcollege-unlock.user.js` — 跳转 API 调用存在
4. `grep "exam-detail" coolcollege-unlock.user.js` — 详情页 URL 路径正确

浏览器手动验证（脚本安装后）：
1. 访问酷学院考试数据页面
2. 确认灰色「作答详情」按钮变为蓝色可点击
3. 点击按钮，确认新标签页打开正确的作答详情页
4. 打开浏览器控制台，确认无报错，有 `[CoolCollege 作答详情解锁]` 日志输出
</verification>

<success_criteria>
1. JavaScript 语法检查通过（`node -c` 无报错）
2. unlockDetailButton 函数存在于脚本中，实现 BTN-01~05 全部功能
3. processRow 在 rowDataMap.set 之后调用 unlockDetailButton
4. 所有 ERR-01~03 错误处理覆盖：Fiber 不存在、submit_id 缺失、选择器失效
5. 脚本可在浏览器中安装运行（需手动验证）
</success_criteria>

<output>
After completion, create `.planning/phases/04-按钮启用与健壮性/04-01-SUMMARY.md`
</output>
